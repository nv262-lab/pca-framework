[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pp-core"
version = "0.0.0"
description = "pp_core package containing pca utilities"
authors = []
readme = ""
dependencies = []
packages = ["pca"]
